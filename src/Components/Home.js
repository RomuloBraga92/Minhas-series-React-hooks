import React, { useState } from 'react';

const home = () => {

    return(
        <div style ={{background: 'black', textAlign: 'center'}} className="vh-100">
            <h1 className="text-white" style={{position: 'relative', top: "40vh", fontSize: "100px"}}>MINHAS SÉRIES</h1>
        </div>
    );

  }

export default home;